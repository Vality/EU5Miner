name: Release Artifacts

on:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-packages --extra=dev

      - name: Build all member wheels
        run: |
          for m in core mcp gui; do
            echo "::group::Building $m"
            (cd packages/$m && uv build --out-dir $PWD/dist)
            echo "::endgroup::"
          done

      - name: Attach all wheels and sdists to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            packages/*/dist/*.whl
            packages/*/dist/*.tar.gz

      - uses: actions/upload-artifact@v7
        with:
          name: release-dist
          path: |
            packages/*/dist/*.whl
            packages/*/dist/*.tar.gz

  publish:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'release'
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v7
      - name: Publish eu5miner to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          (cd packages/core && uv publish)
      - name: Publish eu5miner-mcp to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          (cd packages/mcp && uv publish)
      - name: Publish eu5miner-gui to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          (cd packages/gui && uv publish)
