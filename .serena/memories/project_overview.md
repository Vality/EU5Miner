EU5Miner is a Python 3.12+ library for parsing, indexing, and later editing Europa Universalis V data and mod files. The codebase is layered: install/source discovery, virtual filesystem, generic format readers, semantic parsing, then typed domain adapters under src/eu5miner/domains. Tests prefer real-file coverage against a local EU5 install and focused incremental adapters over speculative schema design.